1.Binary Exponentiation:(a^b)
    a %= MOD;
    while(b > 0)
    {
        if(b % 2 == 1)
        {
        ans = (ans * a) % MOD;
        }
        a = (a * a) % MOD;
        b /= 2;
2.GCD:
-> standard function
#include <numeric>
cout << gcd(18,12);
-> stl
cout << __gcd(18,12);
-> Recursive version
long long gcd(long long a, long long b)
{
    if(b == 0)
        return a;
    return gcd(b, a % b);
}



2... string to int / int to string

int x = 12345;
string s = to_string(x);

string s = "5678";
int x = stoi(s);
